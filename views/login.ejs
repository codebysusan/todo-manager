<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <script class="" src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="./css/styles.css" />
    <title>
        <%= title %>
    </title>
  </head>
  <body>
    <div class="grid grid-cols-6">
      <div class="col-start-3 col-span-2">
        <%- include('header.ejs') %>
        <h6 class="py-4">Login to your account</h6>
        <form action="/session" method="post">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>" >
            <div class="py-3">
                <label for="email">Email:</label>
                <input type="email" name="email" class="border rounded p-2 border-gray-300 text-gray-900 w-full">
            </div>
            <div class="py-3">
                <label for="password">Password:</label>
                <input type="password" name="password" class="border rounded p-2 border-gray-300 text-gray-900 w-full">
            </div>
            <div class="py-3">
                <button type="submit" class="text-white bg-green-600 px-5 py-3 font-medium rounded hover:bg-green-800">Sign in</button>
            </div>
            
        </form>
    </div>
    </div>
  </body>
</html>
